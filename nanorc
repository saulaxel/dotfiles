## Configuración simple para el editor nano
##
## Se debe configurar nano con --enable-nanorc para que
## este archivo sea leido.
##

## Indentado automático
set autoindent

## Copia automática de seguridad al directorio ""
#lset backup
#set backupdir ""

## Hacer busqueda hacia atras por default
# set backwards

## Usar texto grueso en la cabecera en lugar del texto
## con fondo invertido
set boldtext

## Juego de caracteres que se pueden considerar de cierre
set brackets ""')>]}"

## Hacer busquedas que diferencien minusculas y mayusculas
set casesensitive

## Muestra la posicion del cursor en la barra de estado
#set const

## Usar "cut" por defecto para terminar la línea
# set cut

## Definir el ancho de una línea como ancho de pantalla +- fill
#set fill -25

## Definir juego de caracteres cerrados y abiertos
set matchbrackets "(<[{¿¡)>]}?!"

## Usar la línea vacia del final como un espacio más para
## la edición
set morespace

## Habilita el ratón
set mouse

## Permitir multiples buffers (debe existir la
## configuración --enable-multibuffer para que funcione)
#set multibuffer

## No convertir archivos en formatos DOS/Mac
#set noconvert

## No seguir symlinks cuando se editan archivos
#set nofollow

## No mostrar la ayuda al final de la pantalla
#set hohelp

## No aniadir nuevas líneas al final de los archivos
# set nonewlines

## No representar una línea larga como varias líneas
# set nowrap

## Establecer directorio de trabajo para limitar la
## edición con nano fuera del mismo
#set operatingdir ""

## Preservar las teclas XON y XOFF (^Q y ^S)
#set preserve

## Conjunto de caracteres de cierre de puntuación
set punct "!.?"

## Hacer aparecer una barra de estatus pasajera
#set quickblank

## Arreglar la confución entre Backspace y Delete
#set rebinddelete

## Arreglar la confución de los numeros del teclado
#set rebindkeypad

## Hacer busquedas con expresiones regulares
set regexp

## Lo saltos producidos por la tecla home son mas utiles
set smarthome

## Usar navegación suavizada
set smooth

## Definición de un checador de ortografía externo
#set soekker "aspell -x -c"

## Permitir que nano se suspenda
set suspend

## Usar tabulaciónes de 4 espacios
set tabsize 4

## Convertir tabulaciónes a espacios
set tabstospaces

## Guardar automáticamente al salir
#set tempfile

## Hacer que nano sea para solo lectura (no se pa k la vdd)
#set view

## Representación gráfica de los espacios
set whitespace ". "

## Detectar los extremos de una palabra por medio de la
## puntuación
#set wordbounds

